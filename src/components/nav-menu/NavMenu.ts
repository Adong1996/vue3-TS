// export function indexFind(menus: any[], indexPath: string) {
//   let indexId
//   menus.forEach((menu) => {
//     if (menu.type === 2 && menu.url === indexPath) {
//       indexId = menu.id
//     } else {
//       menu.children.forEach((item: any) => {
//         if (item.type === 2 && item.url === indexPath) {
//           indexId = item.id
//         }
//       })
//     }
//   })
//   return indexId
// }
